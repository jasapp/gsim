(ns gsim.test.machine)
;;  (:use [gsim.machine :only [new-machine]]))

(defn test-new-machine []
  nil)
;;  (let [m (new-machine)]
    ;; check that we start at zero
;;    (assert (= (:location m) {:x 0 :y 0 :z 0}))))
    
(defn run []
  (test-new-machine))
